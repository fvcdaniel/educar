<%- model_class = Assunto -%>
<div class="page-header">
  <h1><%=t '.title', :default => model_class.model_name.human.pluralize %></h1>
</div>
<table class="table table-striped">
  <thead>
    <tr>
      <th><%= model_class.human_attribute_name(:id) %></th>
      <th><%= model_class.human_attribute_name(:nome) %></th>
      <th><%= model_class.human_attribute_name(:materia_id) %></th>
      <th><%= model_class.human_attribute_name(:assunto_id) %></th>
      <th><%=t '.actions', :default => t("helpers.actions") %></th>
    </tr>
  </thead>
  <tbody>
    <% @assuntos.each do |assunto| %>
      <tr>
        <td><%= link_to assunto.id, assunto_path(assunto) %></td>
        <td><%= assunto.nome.titleize %></td>
        <td><%= assunto.materia.nome.titleize %></td>
        <td><%= assunto.assunto.nome.titleize if assunto.assunto %></td>
        <td>
          <%= link_to image_tag('edit.png'), edit_assunto_path(assunto), :class => 'quick-edit',
                  :title => 'editar' %>
          <%= link_to image_tag('delete.png'), assunto_path(assunto), :method => :delete,
                      :data => { :confirm => t('.confirm', :default => t("helpers.links.confirm", 
                      :default => 'Você tem certeza?')) },
                      :class => '', :title => 'deletar' %>
        </td>
      </tr>
    <% end %>
  </tbody>
</table>

<%= link_to t('.new', :default => t("helpers.links.new")),
            new_assunto_path,
            :class => 'btn' %>
