<h1>Editar <%= resource_name.to_s.humanize %></h1>

<%= simple_form_for(resource, :html => { :class => 'form-horizontal', :method => :put }, :as => resource_name, :url => registration_path(resource_name)) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :email, :required => true, :autofocus => true %>
    <%= f.input :password, :autocomplete => "off", :hint => "deixe em branco se não quiser modificar", :required => false %>
    <%= f.input :password_confirmation, :required => false, :label => "Confirmar Password" %>
    <%= f.input :current_password, :hint => "precisamos de sua senha atual para confirmar as alterações", :label => "Senha Atual", :required => true %>
  </div>

  <div class="form-actions">
    <%= f.button :submit, "Atualizar" %>
  </div>
<% end %>

<h3>Cancelar minha Conta</h3>

<p>Insatisfeito? <%= link_to "Cancelar minha conta", registration_path(resource_name), :data => { :confirm => "Você está certo disto?" }, :method => :delete %>.</p>

<%= link_to "Voltar", :back %>
