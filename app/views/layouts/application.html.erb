<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=Edge,chrome=1">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= content_for?(:title) ? yield(:title) : "Educar" %></title>
    <%= csrf_meta_tags %>

    <%= render 'layouts/imports' %>

  </head>
  <body>

    <div class="navbar navbar-fixed-top">
      <div class="navbar-inner">
        <div class="container-fluid">
          <%= render 'layouts/navigation' %>
        </div>
      </div>
    </div>

	<div class="container-fluid">
      <div class="row-fluid">
        <% unless params[:controller].to_s.include? 'devise'%>
          <div class="span3">
            <div class="well sidebar-nav">
              <%= render 'layouts/materias' %>
            </div><!--/.well -->
          </div><!--/span-->
          <div class="span9">
            <div class="yeld-unit">
              <%= bootstrap_flash %>
              <%= yield %>
            </div>
          </div><!--/span-->
        <% else %>
          <div class="span12">
            <div class="span6 offset3" style="margin-bottom: 20 px">
              <%= bootstrap_flash %>
              <%= yield %>
            </div>
          </div>
        <% end %>
        
      </div><!--/row-->

      <hr>
      <%= debug(params) if Rails.env.development? %>
      <footer>
        <p>&copy; Company 2012</p>
      </footer>

    </div>

    <!-- Javascripts
    ================================================== -->
    <!-- Placed at the end of the document so the pages load faster -->
    <%= javascript_include_tag "application" %>

  </body>
</html>
