Use this README file to introduce your application and point to useful places in the API for learning more.
Run "rake doc:app" to generate API documentation for your models, controllers, helpers, and libraries.

RAILS_ENV=production bundle exec rake assets:precompile && git add . && git commit -m "test
e" && git push origin master && cap deploy



    use -E utf-8 command line argument to ruby, or
    set your RUBYOPT environment variable to "-E utf-8"
